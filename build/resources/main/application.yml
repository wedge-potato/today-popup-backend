server:
  port: 8080

management:
  endpoints:
    health:
      show-details: always
    web:
      base-path: /
      path-mapping:
        health: check_health
  health:
    disk-space:
      enabled: false

spring:
  profiles:
    default: local
